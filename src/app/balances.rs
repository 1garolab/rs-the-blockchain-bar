#[path = "../database/state.rs"]
mod state;

pub fn balances_cmd() {
    unimplemented!();
    // println!("Accounts balances:");
    // println!("__________________");
    // println!("");
    // let balances = state::new_state_from_disk();
    // balances.iter().map(|(account, balance)| println!("{}: {}", account, balance));
}
